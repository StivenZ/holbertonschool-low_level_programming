#include <stdio.h>
/**
 * main - prints function name
 * @argc: arguments count
 * @argv: array
 * Return: 0
*/
int main(int argc, char *argv[])
{
	if (argc == 1)
	{
	printf("%s\n", argv[0]);
	}
	return (0);
}
